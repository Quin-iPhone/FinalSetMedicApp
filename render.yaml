startCommand: gunicorn app:app --bind 0.0.0.0:5000
services:
  - type: web
    name: set-medic-app
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: FLASK_ENV
        value: production
      - key: FLASK_APP
        value: app.py
